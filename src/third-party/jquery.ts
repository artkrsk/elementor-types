/**
 * jQuery Integration Types
 * Extended jQuery types and augmentations for Elementor
 */

/// <reference types="jquery" />

/**
 * Re-export jQuery types for convenience
 * Note: jQuery types are available globally via reference directive
 */
export {};
